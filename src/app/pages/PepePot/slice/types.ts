/* --- STATE --- */
export interface PepePotState {
  betAmount: number;
  daoAmount: number;
  potAmount: number;
  burnAmount: number;
  oddsIncrease: number;
  currentOdds: number;
  currentPot: number;
  pending: boolean;
}
